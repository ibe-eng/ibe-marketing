---
interface Props {
  word: string;
}
const { word } = Astro.props;
---

<span class="oval-wrap">
  <span class="oval-word">{word}</span>
  <svg
    class="oval-svg"
    viewBox="0 0 300 80"
    preserveAspectRatio="none"
    aria-hidden="true"
    focusable="false"
  >
    <path
      d="M 4,40 C 4,15 60,5 150,5 C 240,5 296,15 296,40 C 296,65 240,75 150,75 C 60,75 4,65 4,40 C 4,34 7,29 12,27"
      fill="none"
      stroke="var(--color-accent)"
      vector-effect="non-scaling-stroke"
      stroke-linecap="round"
      stroke-linejoin="round"
    />
  </svg>
</span>

<style>
  .oval-wrap {
    position: relative;
    display: inline-block;
    /* Reserve the annotation's space in the line, including at wraps. */
    padding: 0.1em 0.35em;
    white-space: nowrap;
  }

  .oval-word {
    position: relative;
    z-index: 1;
  }

  .oval-svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    overflow: visible;
  }

  .oval-svg path {
    /* A bold marker loop with an overlapping finish; scale with the type,
       not with the SVG's horizontal stretch on longer words. */
    stroke-width: clamp(3px, 0.09em, 6px);
  }
</style>
